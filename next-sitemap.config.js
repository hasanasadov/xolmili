// next-sitemap.config.js

/** @type {import('next-sitemap').IConfig} */
const config = {
  siteUrl: "https://xolmili.vercel.app",
  generateRobotsTxt: true,
  sitemapSize: 7000,
};

module.exports = config;
